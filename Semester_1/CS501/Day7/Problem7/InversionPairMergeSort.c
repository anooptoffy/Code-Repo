#include <stdio.h>


/*
 * Inversion Pair   Can we do better ? O(nlogn)
 * 
 *  a0, a1, a2, ... , an-1 is in the increasing order
 *  if i < j and ai > aj forms the inversion pair
 *
 *  Unsorted
 */


int main(int argc, char const *argv[])
{
	int a[] = {4, 5, 2, 6, 1};
	int size = sizeof(a)/sizeof(a[0]);
	return 0;
}