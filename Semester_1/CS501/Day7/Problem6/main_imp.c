#include <stdio.h>

/*
 *
 *
 * max(aj - ai)
 * i <= j <= i+l
 * using naive approach that has complexity O(n*l) Improving Further Is O(n) possible ?
 *
 */

int main(int argc, char *argv[]){
	int a[] = {10, 11, 7, 10, 6};
 	int size = sizeof(a)/sizeof(a[0]);
	int l = 3;
	int max = -1;


	return 0;
}